<div class="card">
    <div class="card-body">
        <small>Data de publicação: {{postagem.data}}</small>
        <h1>{{postagem.titulo}}</h1>
        <h6>{{postagem.descricao}}</h6>
        <hr>
        {{postagem.conteudo}}
    </div>
</div>