<h1>{{categoria.nome}}</h1>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <small>Data de publicação: {{data}}</small>
        <hr>
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais...</button></a>
    </div>
</div>

{{else}}
    <h6>Nenhuma postagem recente</h6>
{{/each}}